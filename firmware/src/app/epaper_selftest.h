#pragma once
// ============================================================
// 应用层 · B1 实机点屏自检（对应开发约定§10 清单）
// 四色测试图：黑白棋盘格验证黑白平面寻址，红/黄验证带验证彩色平面，
// 四角 L 形角标验证整幅寻址（分辨率口径不符时右/下留白边）。
// 流程：epaper::init → 渲染 → 全刷（实测记录时长）→ 屏体休眠。
// ============================================================

namespace epaper_selftest {

void run();  // B1 全流程（全刷约 21s 期间阻塞调用方，勿断电）

}  // namespace epaper_selftest
