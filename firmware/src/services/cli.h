#pragma once
// ============================================================
// 服务层 · 串口 CLI（M2）
// 行式命令（\n 结束，\r 忽略），非阻塞读串口、loop 内分发。
// 已知限制：SSID/密码含空格或逗号时无法经此输入（配网主通道
// Web Serial 下发，见设计文档；本 CLI 为调机/产线兜底）。
// ============================================================

namespace cli {

void begin();  // 打印就绪提示
void poll();   // loop 调用：非阻塞读行并分发

}  // namespace cli
