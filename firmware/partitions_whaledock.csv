# Name,   Type, SubType,  Offset,   Size
# NOTE: ASCII-only comments in this file! PlatformIO's Python builder parses it
# line by line using the OS locale codec (GBK on zh-CN Windows), so UTF-8
# comments crash the build with UnicodeDecodeError at checkprogsize.
#
# 16MB flash layout: dual OTA (2 x 6.4MB) + LittleFS + assets + coredump.
# Header alignment: the dev guide's original table overlapped phy_init/ota_0;
# here nvs 16K + otadata 8K + phy 4K fill 0x9000..0x10000 so ota_0 keeps the
# guide's 0x10000 start (see ESP32-S3 dev guide section 5 + errata in docs).
nvs,      data, nvs,      0x9000,   0x4000
otadata,  data, ota,      0xD000,   0x2000
phy_init, data, phy,      0xF000,   0x1000
ota_0,    app,  ota_0,    0x10000,  0x640000
ota_1,    app,  ota_1,    0x650000, 0x640000
lfs,      data, spiffs,   0xC90000, 0x2F0000
assets,   data, spiffs,   0xF80000, 0x70000
coredump, data, coredump, 0xFF0000, 0x10000
